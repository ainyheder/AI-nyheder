# Python
__pycache__/
*.pyc

# Editor/OS
.vscode/
.idea/
.DS_Store

# Claudes midlertidige mappe
_to_delete/

# Natsessionens testværktøj. jsdom installeres for at kunne indlæse forsiden
# og artikelsiderne med rigtige data, før noget bliver skrevet. Det hører ikke
# til siden og skal aldrig pushes - 26 MB og 1.807 filer.
node_modules/
package.json
package-lock.json

# Natsessionens låsefil - kun til at holde de to natlige kørsler fra hinanden
_redaktion/.nat-koerer

# Andre git-repoer, der er havnet inde i denne mappe. De hører ikke til siden,
# og de må ALDRIG committes med: `AI-nyheder/` er en klon af præcis dette repo
# (68 MB, 459 filer) og `WIDU-OS/` er et selvstændigt projekt. Uden de her linjer
# kan GitHub Desktop tilbyde at committe dem, og så ligger repoet inde i sig selv.
# Linjerne er en sele, ikke en løsning — begge mapper hører til uden for AI NEWS.
AI-nyheder/
WIDU-OS/
